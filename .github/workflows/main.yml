name: CI

on:
  push:
    branches: ["*"]
  pull_request:
    branches: ["master"]

  workflow_dispatch:

jobs:
  deploy-backend:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Build
        run: docker-compose build
